#include "main.h"

/**
 * rev_string - Reverses a string
 * @s: Input string
 * Return: String in reverse
 */
void print_rev(char *s)
{
	char z;

	z = 'z';
	while (z >= 's')
	{
		_putchar(z);
		z--;
	}
}
